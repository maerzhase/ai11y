{
	"name": "ui4ai",
	"author": "maerzhase3000",
	"private": true,
	"scripts": {
		"dev": "turbo run dev",
		"build": "turbo run build",
		"build:demo": "pnpm install && turbo run build --filter=ui4ai-demo",
		"build:server": "pnpm install && turbo run build --filter=ui4ai-server-app",
		"watch": "turbo run watch",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.10",
		"turbo": "2.7.1"
	},
	"packageManager": "pnpm@9.0.0",
	"engines": {
		"node": ">=18"
	}
}
