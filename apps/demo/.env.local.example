# AI11y agent API endpoint. Default: /api/ai11y/agent (built-in serverless route)
# Override only if using an external agent server.
# NEXT_PUBLIC_AI11Y_API_ENDPOINT=/api/ai11y/agent

# Required for LLM agent (built-in route). Without this, the client falls back to rule-based agent.
# OPENAI_API_KEY=sk-...

# Optional LLM settings
# LLM_MODEL=gpt-5-nano
# LLM_TEMPERATURE=0.7
# OPENAI_BASE_URL=
